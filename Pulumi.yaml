name: slatedb-pulumi
description: Pulumi scripts to manage SlateDB committer access to various infrastructure resources.
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  github:owner:
    value: slatedb
  pulumi:tags:
    value:
      pulumi:template: python
